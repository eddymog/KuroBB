# The running app — see kurobb-design.md §03. No CORS_ORIGIN: there is only one
# origin (React Router mounted in this same Express process).
DATABASE_URL=postgres://kurobb:kurobb@localhost:5433/kurobb
SESSION_COOKIE_SECRET=replace-with-output-of-openssl-rand-hex-32
SESSION_TTL=30d
PORT=3000
NODE_ENV=development
